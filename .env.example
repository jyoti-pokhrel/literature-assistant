SEMANTIC_SCHOLAR_API_KEY = 
TAVILY_API_KEY = 
OPENALEX_API_KEY = 
OPENROUTER_API_KEY = 

MODEL_NAME = qwen/qwen-2.5-7b-instruct
SYNTHESIS_MODEL_PRIMARY = qwen/qwen-2.5-7b-instruct
SYNTHESIS_MODEL_FALLBACK = qwen/qwen-2.5-72b-instruct
HF_TOKEN = 
EMBEDDING_MODEL=all-MiniLM-L6-v2
EMBEDDING_FALLBACK_MODEL=all-mpnet-base-v2

# qwen/qwen-2.5-7b-instruct
# API Key
API_KEY=
API_KEY_NAME=


#JWT Settings
SECRET_KEY=
ALGORITHM=
ACCESS_TOKEN_EXPIRE_MINUTES=


#MongoDB
MONGODB_URL=
DB_NAME=



# === Authentication ===
# Set to "1" to bypass auth and return a synthetic admin user when no
# Authorization header is sent. Useful for localhost dev only — leave at "0"
# anywhere you want real auth enforcement (staging, production, demos).
AUTH_DEV_BYPASS=0

# Resend API (used for OTP + password reset emails). If empty, emails are logged instead of sent.
RESEND_API_KEY=
EMAIL_FROM=onboarding@resend.dev

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://127.0.0.1:8000/auth/google/callback

# URLs used by email links and the OAuth redirect target
FRONTEND_URL=http://127.0.0.1:8000
BACKEND_URL=http://127.0.0.1:8000
